version: '3.4'

networks:
  hyperf:

services:
  site:
    image: ${IMAGE_NAME}
    container_name: hyperf-demo
    environment:
      - APP_ENV=${APP_ENV}
      - TIMEZONE=Asia/Shanghai
    ports:
      - "${SERVICE_PORT}:9501"
    networks:
      - hyperf